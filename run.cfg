# mcu_simulater 运行配置示例
# 关键字: elf(必填) app(可选) n(每步预算) max_steps(run上限,0=直到quit/EOF) rx_port(stdin注入USART口)
# `#` 或 `;` 开头为注释；支持 key = value

elf = D:\project\mcu\oop\joc-base\build_rel\stm32f407_minimal.elf
app = D:\project\mcu\oop\drv-bringup-app-rust\app.bin     # 应用分区(轨B); 去掉此行使不带 app 跑
n = 400000
max_steps = 0
rx_port = 1
