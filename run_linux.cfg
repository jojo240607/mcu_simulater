# mcu_simulater Linux 运行配置（等价 run.cfg 的 Windows 版）
# 关键字: elf(必填) app(可选) n(每步预算) max_steps(run上限,0=直到quit/EOF) rx_port(stdin注入USART口)
# `#` 或 `;` 开头为注释；支持 key = value

elf = /home/ubuntu/work/joc-base/build_rel/stm32f407_minimal.elf
app = /home/ubuntu/work/joc-rtos-app-sdk/app.bin     # 应用分区(轨B): rtos-app-sdk 的 demo-app
n = 400000
max_steps = 0
rx_port = 1
